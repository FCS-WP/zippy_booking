import React, { useEffect } from "react";
import ReactDOM from "react-dom";
import Header from "../../Components/Layouts/Header";

// Zippy Dashboard

const Bookings = () => {
  return <Header title={"shin"} />;
};

export default Bookings;
